@page "/fetchdata"
@using MudBlazorPlayground.Shared.Components
@inject HttpClient Http

<PageTitle>Weather forecast</PageTitle>

<h1>Weather forecast</h1>

<p>This component demonstrates fetching data from the server.</p>

@if (forecasts == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <CardList TItem="WeatherForecast" Items=@forecasts>
        <CardTitle>@context.Date</CardTitle>
        <CardBody>
            <p>High: @context.TemperatureF</p>
            <p>@context.Summary</p>
        </CardBody>
        <CardButtonLabel>Click me plz</CardButtonLabel>
    </CardList>
    
}

@code {
    private List<WeatherForecast>? forecasts;

    protected override async Task OnInitializedAsync()
    {
        var forecastsArray = await Http.GetFromJsonAsync<WeatherForecast[]>("sample-data/weather.json");
        forecasts = forecastsArray!.ToList();
    }

    public class WeatherForecast
    {
        public DateTime Date { get; set; }

        public int TemperatureC { get; set; }

        public string? Summary { get; set; }

        public int TemperatureF => 32 + (int)(TemperatureC / 0.5556);
    }
}
